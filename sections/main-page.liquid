<section class="main-page section-padding">
  <div class="container" style="max-width:800px;">
    <h1 style="font-family:var(--font-heading); font-size:clamp(2rem,4vw,3rem); text-align:center; margin-bottom:32px;">{{ page.title }}</h1>
    <div class="page-content" style="font-size:15px; line-height:1.8; color:var(--color-text-muted);">
      {{ page.content }}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Page",
  "settings": []
}
{% endschema %}
