<section class="featured-by">
  <div class="featured-by__inner">
    <p class="featured-by__label">{{ section.settings.title }}</p>
    <div class="featured-by__marquee-wrapper">
      <div class="featured-by__marquee">
        {%- for block in section.blocks -%}
          <span class="featured-by__name" {{ block.shopify_attributes }}>{{ block.settings.name }}</span>
          <span class="featured-by__dot">·</span>
        {%- endfor -%}
        <!-- Duplicate for seamless loop -->
        {%- for block in section.blocks -%}
          <span class="featured-by__name" {{ block.shopify_attributes }}>{{ block.settings.name }}</span>
          <span class="featured-by__dot">·</span>
        {%- endfor -%}
      </div>
    </div>
  </div>
</section>

<style>
  .featured-by {
    background: #E9DECE; /* Sand — same as trust-and-stats */
    padding: 52px 20px;
    border-top: 1px solid rgba(29,29,29,0.06);
    border-bottom: 1px solid rgba(29,29,29,0.06);
  }
  .featured-by__inner {
    max-width: var(--container-max);
    margin: 0 auto;
    text-align: center;
  }
  .featured-by__label {
    font-family: 'Montserrat', sans-serif;
    font-size: 12px;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: #8B7355;
    margin-bottom: 28px;
    font-weight: 600;
  }
  /* Marquee wrapper */
  .featured-by__marquee-wrapper {
    overflow: hidden;
    position: relative;
    width: 100%;
  }
  .featured-by__marquee-wrapper::before,
  .featured-by__marquee-wrapper::after {
    content: '';
    position: absolute;
    top: 0;
    bottom: 0;
    width: 80px;
    z-index: 2;
    pointer-events: none;
  }
  .featured-by__marquee-wrapper::before {
    left: 0;
    background: linear-gradient(to right, #E9DECE, transparent);
  }
  .featured-by__marquee-wrapper::after {
    right: 0;
    background: linear-gradient(to left, #E9DECE, transparent);
  }
  .featured-by__marquee {
    display: flex;
    align-items: center;
    gap: 32px;
    animation: marqueeScroll 20s linear infinite;
    white-space: nowrap;
    will-change: transform;
  }
  @keyframes marqueeScroll {
    0%   { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }
  .featured-by__marquee:hover {
    animation-play-state: paused;
  }
  .featured-by__dot {
    color: #8B7355;
    opacity: 0.6;
    font-size: 24px;
  }
  .featured-by__name {
    font-family: 'Cormorant Garamond', serif;
    font-size: clamp(1.7rem, 4.5vw, 2.3rem);
    font-style: italic;
    color: #1D1D1D;
    opacity: 0.4;
    font-weight: 500;
    letter-spacing: 0.02em;
    flex-shrink: 0;
    transition: opacity 0.3s;
  }
  .featured-by__name:hover {
    opacity: 0.8;
  }
</style>

{% schema %}
{
  "name": "Featured By",
  "settings": [
    { "type": "text", "id": "title", "default": "TRUSTED & FEATURED BY", "label": "Title" }
  ],
  "blocks": [
    {
      "type": "brand",
      "name": "Brand",
      "settings": [
        { "type": "text", "id": "name", "default": "Vogue", "label": "Brand name" }
      ]
    }
  ],
  "presets": [
    {
      "name": "Featured By",
      "blocks": [
        { "type": "brand", "settings": { "name": "Vogue" } },
        { "type": "brand", "settings": { "name": "Elle" } },
        { "type": "brand", "settings": { "name": "Brides" } },
        { "type": "brand", "settings": { "name": "Harper's Bazaar" } },
        { "type": "brand", "settings": { "name": "InStyle" } },
        { "type": "brand", "settings": { "name": "Cosmopolitan" } },
        { "type": "brand", "settings": { "name": "Glamour" } }
      ]
    }
  ]
}
{% endschema %}
